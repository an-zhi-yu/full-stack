# 公共配置；环境差异见 application-dev.properties / application-prod.properties
spring.application.name=blog-api

# 默认使用 dev，生产部署时改为 prod：java -jar app.jar --spring.profiles.active=prod
spring.profiles.active=dev
